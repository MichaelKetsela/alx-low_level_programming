#include <stdio.h>
/**
 * main - entry point
 * Description:'print all number of base 16 in lowercase'
 * Return: always 0
 */
int main(void)
{
	int h
    h;

	for (h = 48; h <= 57; h++)
	{
		putchar(h);
	}

	for (h = 97; h <= 102; h++)
	{
		putchar(h);
	}
	putchar(10);
	return (0);
}
