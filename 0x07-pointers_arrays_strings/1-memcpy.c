#include "main.h"
/**
* _memcpy - Copies memory area
*
* @dest: destination where to copy the memory to
* @n: the number of bytes
* @src: source that the memory area to copy from
*
* Return: a pointer to @dest
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
int j = i;
memcpy(dest, src, j );
Return ( dest );
}
